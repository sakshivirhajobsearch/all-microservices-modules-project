server:
  port: 8087

spring:
  application:
    name: resilience4j

management:
  endpoints:
    web:
      exposure:
        include: health,metrics
  endpoint:
    health:
      show-details: always
