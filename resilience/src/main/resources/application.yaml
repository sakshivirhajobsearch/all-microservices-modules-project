server:
  port: 8088

spring:
  application:
    name: resilience4j

management:
  endpoints:
    web:
      exposure:
        include: health,metrics
  endpoint:
    health:
      show-details: always
